整个项目的结构：
  1. src代码目录
  2. resource资源目录
  3. notes笔记目录
  4. config配置文件

优化的地方：
  1. 设置文件运行程序图标

在那个组件上加入一个监听的东西：一旦按钮显示，就开始播放动画，一旦按钮被隐藏，就关闭动画
不需要像1.0版本的复杂
等我出到3.0版本的时候，我就可以把1.0版本封存了
事件绑定，绑定的是键盘、鼠标这些
我想实验下，是不是我想实现的这种方式可以绑定configure
其实可以这样做：
  1. 把播放动画不要放在初始化里面，而是放在create_surface函数里面
  2. 这样，一旦从后面的界面返回的时候，就会重新创建surface，就可以实现这种方式
create_surface函数是进行了放置组件的函数，还包含了组件播放动画的功能
__init__应该在程序开始的时候就把所有的组件都创建好